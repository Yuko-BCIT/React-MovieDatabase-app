// import { useParams } from "react-router-dom";

const Individual = () => {
//   const { id } = useParams();
  return (
    <section>
      <h2>Hello this is individual page</h2>
      {/* <p>{id}</p> */}
    </section>
  );
};

export default Individual;
